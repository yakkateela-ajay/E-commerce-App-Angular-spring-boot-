import { NarzoPhoneFilterPipe } from './narzo-phone-filter.pipe';

describe('NarzoPhoneFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NarzoPhoneFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
