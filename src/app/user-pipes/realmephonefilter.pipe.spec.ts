import { RealmephonefilterPipe } from './realmephonefilter.pipe';

describe('RealmephonefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RealmephonefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
