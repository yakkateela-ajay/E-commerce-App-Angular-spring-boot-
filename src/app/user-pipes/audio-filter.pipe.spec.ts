import { AudioFilterPipe } from './audio-filter.pipe';

describe('AudioFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AudioFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
